<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.CenterMapper">

    <insert id="storeRegi" useGeneratedKeys="true" keyProperty="id">
        insert into CENTER (id, name, address, info, phoneNumber, file)
        values
        (#{memberId}, #{startDate}, #{endDate}, #{remainingPT})
    </insert>



</mapper>